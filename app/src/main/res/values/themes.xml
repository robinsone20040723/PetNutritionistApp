<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App 主要主題：Material3，無 ActionBar -->
    <style name="Theme.PetNutritionistApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- 如有 colors.xml 的 md_theme_* 會自動套用；沒有也可運作 -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="colorSecondary">@color/md_theme_light_secondary</item>
        <item name="colorSurface">@color/md_theme_light_surface</item>
        <item name="colorOnSurface">@color/md_theme_light_onSurface</item>

        <!-- TextInputLayout 統一樣式（可選） -->
        <item name="textInputStyle">@style/Widget.M3.TextInputLayout</item>
    </style>

    <!-- 全螢幕 Dialog（只在這裡定義，不要在 styles.xml 再放一份） -->
    <style name="FullScreenDialogStyle" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- TextInputLayout 外觀（可選） -->
    <style name="Widget.M3.TextInputLayout" parent="Widget.Material3.TextInputLayout.FilledBox">
        <item name="boxBackgroundMode">filled</item>
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
    </style>

</resources>
